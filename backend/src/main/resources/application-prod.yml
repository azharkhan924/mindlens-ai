spring:
  datasource:
    url: jdbc:postgresql://db.kzrlpwedhqrwxbgdqivc.supabase.co:5432/postgres?sslmode=require
    username: postgres
    password: ${DB_PASSWORD}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false

# Overrides for production
h2:
  console:
    enabled: false

