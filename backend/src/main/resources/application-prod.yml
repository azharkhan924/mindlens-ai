spring:
  datasource:
    url: jdbc:postgresql://db.kzrlpwedhqrwxbgdqivc.supabase.co:5432/postgres
    username: postgres
    password: ${DB_PASSWORD}
    driver-class-name: org.postgresql.Driver
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
    show-sql: false

# Overrides for production
h2:
  console:
    enabled: false
